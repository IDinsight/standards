# OpenAI Codex adapter. This file supplies the interface metadata Codex shows in its
# agent picker. The skill's actual instructions live in ../SKILL.md, which the Agent
# Skills client installs alongside this file and loads when the skill runs.
interface:
  default_prompt: "Use $architect to turn the completed scope into a buildable technical design. Resolve material technical decisions, define relevant contracts and technical acceptance criteria, avoid changing scope or implementing code, then hand off to Auditor after initial greenfield architecture or Developer after brownfield architecture and downstream architecture rework when project context remains current."
  display_name: "Architect"
  short_description: "Turn completed scope into a buildable technical design."

policy:
  allow_implicit_invocation: false
  products:
    - CODEX
